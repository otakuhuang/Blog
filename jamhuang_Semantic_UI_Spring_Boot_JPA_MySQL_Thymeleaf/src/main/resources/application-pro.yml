spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jamhuang_blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT
    username: root
    password: am2w5tfx

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.jamhuang: info
  file:
    name: log/blog-pro.log

server:
  port: 80
